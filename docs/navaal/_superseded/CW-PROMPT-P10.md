# CW — PHASE 10 BRIEF: THE CAPTURE, AT LAST — THEN THREE READS

Paste this whole file. It replaces `CW-PROMPT-P9.md` (moved to `_superseded/`).

**15 → 4 is the number this phase was for, and you produced it.** Two things in that count are
worth more than the number: FR1 proved fixed on the exact trigger that broke it (the store you
renamed after install), and FR9 proved fixed **without** the reset, by a 105-second poll across
twenty re-renders. That is how a fix is proved. And you caught what CC's own live read missed: the
splash says 100 where the card says 97, and *"This product: 21/100"* is the store score wearing a
product's label. **That relabel made a misplaced number into a false claim** — the same shape as
`live` and `Live on your storefront` before it — and you said so. CC has all four as Part A, and
nothing else comes first for it.

Also verified from outside: the five slots are live exactly as §5.6; `/terms` carries *"whatever
your billing date"*; `/privacy` carries `International transfers`; production `1e6873e`.

---

## ORIENT

`CW-STANDING-PROMPT.md` · `06-QUEUE.md` INBOX (top two rows) · `12-OFFER.md` §4 for captions.
`curl -s "https://app.navaal.ai/api/build-info?cb=$RANDOM"`.

Store rules: dev2 and qa-fresh frozen until you post `CAPTURE COMPLETE`. `navaal-ttv-03` is not
frozen. EBS read-only. Real merchants public pages only.

---

## TASK 1 — THE CAPTURE (gated on CC's Part A sha + the reset)

When CC posts the Part A sha **and** confirms the First-run reset ran on `navaal-qa-fresh`:

1. `/api/build-info` shows the sha live.
2. Re-read the qa-fresh splash: **three rows, three different product scores** (or no score on the
   row); *"3 credits of the 97 you have left"* — 97, not 100; the row `[Review]` on a draft opens a
   review with approve/publish on it. Quote all three. **If any fails, no capture; post and stop.**
3. `contentpilot-dev2`: four populations still agree; usage percent is not `0%` at 19/4000; the
   `Attention` / *"product is a Shopify draft"* row still reads as you last saw it.
4. Capture **04** from qa-fresh; **01/06, 02/07, 03/08, 05** from dev2. Nothing else touches
   either store during the window.
5. Look at every PNG. One sentence per unusable frame. Upload only with **≥3 clean desktop**;
   captions from `12-OFFER.md` §4 language; no pricing, no PII, no Shopify chrome.
6. Read the live listing back on a fresh load — the images showing are the ones you uploaded.
7. Post `CAPTURE COMPLETE`. Both freezes lift.

---

## TASK 2 — THE LEGAL PAGES, RE-READ (ungated)

Both are live. Quote the reset sentence from `/terms` **and** from the plans page inside the app
(dev store, any) — they must be identical. Quote the `International transfers` paragraph's first
sentence and confirm each processor's DPA link answers 200 (CC says seven). Re-run yesterday's
expect-0 list on both pages.

## TASK 3 — F3 ON A MULTI-VARIANT PRODUCT (ungated)

On `navaal-ttv-03`, a product with two or more variants whose barcode is on variant 2 only (create
one if none). `/app/attention` must not grade it "no barcode". Quote the row.

## TASK 4 — THE SWEEP

Standing. §5.6 lines expect-1, displaced lines expect-0, privacy host, `save 20%` 3, `14-day` ≥3.

## TASK 5 — WHEN THE CLOCKS SAY SO

Webhooks on or after 16 Sep. App Store rank 21 Sep, real browser. The `navaal.ai` 301 check once the
owner has done the Hostinger session.

## STILL OWNER-BLOCKED

Hostinger session · `REMEDIATION_LOCKED_SHOPS` · F10 (one dev store public + Bing key; it also
unblocks CC's Lighthouse number) · P0.10 · B0.1.

---

## ORDER

Task 2 → Task 3 → Task 4 → **wait** for CC's sha → Task 1 → Task 5 when due.

Claim-vs-screen first, verbatim, *could not read* when you could not. Queue under
`## PHASE 10 — CW`, no IDs.

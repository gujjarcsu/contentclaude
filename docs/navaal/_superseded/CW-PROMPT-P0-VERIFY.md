# CW — P0 FOLLOW-THROUGH: PROVE THE RELEASE, THEN THE TASKS YOU PARKED

Paste this whole file.

**Your Task 0 was the finding of the project.** Not because it was clever — because it was
disciplined: you established the commit fact from the repo before reading the Versions page, you
read the page's own timezone instead of assuming one, you reported 0b as *could not be run, not a
pass* when the storefront password stopped you, and you named the two things you could not prove
rather than letting the report imply them. Three workers read green deploys all week. You opened
the one page none of us did.

It is false green **#12** now, in the constitution and in CC's standing prompt as a mandatory ship-gate
step. CC has a P0 brief to release the version; the owner has a one-line item if the CLI needs a
login. Your job is to prove the release landed, from the outside, the same way you found the gap.

---

## TASK 0 — PROVE THE RELEASE (gated on CC posting a version number)

Watch the queue for CC's line naming the **new version number and created time**. Then:

1. **Versions page.** Read the active version. Quote its name and *Created* line verbatim, with the
   `+0000`. It must be newer than `navaal-seo-geo-content-15` and its created time must be after
   **11:26:53 UTC 9 Sep** — the commit that escaped the block. If it is still v15, CC's release did
   not happen, whatever CC's terminal said.
2. **The side-effect read.** Your sweep, on the public listing: `save 17%` and `7-day`. Last time
   **3 and 3** on the page, **0** in every field we author. After a real release they go to **0** on
   the page with nobody editing anything, because Shopify regenerates the pricing display from the
   released billing config. Report both integers. If they are still 3, the release did not carry the
   billing config — that is a finding, not a wait.
3. **Pricing details section.** Re-read it. The plan cards should now show **95.90 / 287.90 /
   767.90** and **14-day**. Quote one card verbatim.
4. **0b, if the owner has unblocked it.** If the storefront password has been entered in your
   browser or turned off on `contentpilot-dev2`, read the rendered product page HTML for a FAQ
   containing a special character and say whether it is `&lt;`-escaped. If still blocked, one line:
   *still blocked on the storefront password*. Not a pass.

**Do not upload, edit, or capture anything under Task 0.** Read and report.

---

## TASK 1 — THEN THE THREE YOU PARKED, IN THIS ORDER

You left Tasks 1–3 of `CW-PROMPT-P6B.md` untouched, correctly. They are unchanged:

**Task 2 first — enumerate every number on Home and Products with its exact label**, on the dev
store, beside the store's real product count. CC has fixed this class one instance at a time and
missed it twice. The table you produce is what lets it fix the screen in one pass. Frame 01 alone
had: `Autopilot optimized 15 new products` · `30 products optimized` · `0 drafts awaiting review` ·
`across 14 products sampled` · `Total Products 32` · `AI Content Published 30`.

**Then Task 1 — set the store up for one final capture.** Several drafts pending so Review is not
empty; write into the queue exactly what state the store is in and that captures must run with
nothing else mutating the store. Do not capture — that stays blocked on CC's count fix and the
nameless `Welcome back!` on frame 04.

**Then Task 3 — the full sweep**, which now includes the two release checks above.

---

## STILL OWNER-BLOCKED

The W1 post upload. Unchanged.

---

Report the difference between claim and screen first. Quote verbatim. Say *could not read* when you
could not read. INBOX, **no ID**. Re-read `/api/build-info` and the Versions page mid-session — both
have moved under you before.
